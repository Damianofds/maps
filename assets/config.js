window.MAPS_CONFIG = window.MAPS_CONFIG || {};

// GitHub Pages is served over HTTPS. Set this to an HTTPS GeoServer endpoint
// with CORS enabled before publishing, for example:
// window.MAPS_CONFIG.geoserverBase = "https://maps.example.com/geoserver";
window.MAPS_CONFIG.geoserverBase = window.MAPS_CONFIG.geoserverBase || "/geoserver";
